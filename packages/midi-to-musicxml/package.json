{
  "name": "@midi-to-xml/midi-to-musicxml",
  "version": "0.1.0",
  "description": "Convert MIDI files directly to MusicXML with meter-aware rhythm cleanup.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "npm run clean && tsc -p tsconfig.json",
    "check": "npm run typecheck && npm run build && find dist test -name '*.js' -print0 | xargs -0 -n1 node --check",
    "clean": "rm -rf dist",
    "prepack": "npm run build",
    "test": "npm run build && node --test \"test/**/*.test.js\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@tonejs/midi": "^2.0.28"
  }
}
