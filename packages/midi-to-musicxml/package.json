{
  "name": "@midi-to-xml/midi-to-musicxml",
  "version": "0.1.0",
  "description": "Convert MIDI files to MusicXML with optional quantization via MuseScore.",
  "type": "module",
  "main": "src/index.js",
  "exports": {
    ".": "./src/index.js"
  },
  "scripts": {
    "check": "find src test -name '*.js' -print0 | xargs -0 -n1 node --check",
    "test": "node --test \"test/**/*.test.js\""
  },
  "dependencies": {
    "@tonejs/midi": "^2.0.28"
  }
}
