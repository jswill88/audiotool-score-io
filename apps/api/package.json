{
  "name": "@midi-to-xml/api",
  "version": "0.1.0",
  "private": true,
  "description": "HTTP API for MIDI, MusicXML, and Audiotool conversion workflows.",
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "npm run typecheck && npm run build && find dist -name '*.js' -print0 | xargs -0 -n1 node --check",
    "start": "node dist/server.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@midi-to-xml/audiotool-to-midi": "0.1.0",
    "@midi-to-xml/midi-to-musicxml": "0.1.0",
    "adm-zip": "^0.5.17",
    "dotenv": "^17.4.2",
    "express": "^4.18.4",
    "multer": "^1.4.5-lts.1"
  }
}
